<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.itstimetomovie" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/appbar</item>
        <item name="colorPrimaryVariant">@color/principal</item>
        <item name="colorOnPrimary">@color/letras</item>
        <item name="android:textColor">@color/letras</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/appbar</item>
        <item name="colorSecondaryVariant">@color/letras</item>
        <item name="colorOnSecondary">@color/letras</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
</resources>